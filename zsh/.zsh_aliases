# Custom Aliases
alias update='if sudo apt list --upgradable | grep -q uogradable; then sudo apt update && sudo apt upgrade - y && sudo apt autoremove -y; else echo "System is already up to date."; fi'
alias gs="git status"
alias ll="ls -la"
alias grep="grep --color=auto"
alias clean='sudo apt autoremove --purge && sudo apt autoclean && sudo apt clean'
